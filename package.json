{
  "name": "polyx",
  "version": "0.1.0",
  "private": true,
  "description": "JSX to Custom Elements compiler",
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^4.0.18",
    "happy-dom": "^20.6.1",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  }
}
